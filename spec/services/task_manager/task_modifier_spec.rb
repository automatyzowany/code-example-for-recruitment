require 'rails_helper'

RSpec.describe 'TaskModifier' do
  # it 'should modify the name field when this field exists' do
  #   params = { data: {'701'=>{ name: 'test2nw', user_id: '2'}}, board_id:'17', user_id: '2', id: '701'}
  #   board = create(:board)
  #   user = create(:user)
  #   task = create(:test)

  #   result = TaskManager::TaskModifier.call(params, board, task.id)
  #   expect(Test.find(701)).to include('test2nw')
  # end
end
